meta:
  toolchain:
    name: python
    version: "3.12"

build:
  commands:
    - pip install -r requirements.txt

run:
  command: python check.py

env:
  - name: BOT_TOKEN
    secret: true

  - name: CHAT_ID
    secret: true